<%= form_for(@destinations) do |f| %>
  <% if @destinations.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@destinations.errors.count, "error") %> prohibited this destination from being saved:</h2>

      <ul>
      <% @destinations.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :instructions %><br>
    <%= f.text_field :instructions %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>